type Mutation {
  # User
  addUser(
    name: String
    age: Int
  ): User

  # Book
  addBook(
    name: String
    genre: String
    userId: String
  ): Book

  # Worker
  registerWorker(worker: RegisterWorkerInput!): ReturnedRegisteredWorker
}