type Query {
  # User
  user(id: ID!): User
  users: [User]

  # Book
  book(id: ID!): Book
  books: [Book]

  # Worker
  loginWorker(loginInput: LoginInput!): ReturnedRegisteredWorker

  # Client
  loginClient(loginInput: LoginInput!): ReturnedRegisteredClient

  # Admin
  loginAdmin(loginInput: LoginInput!): ReturnedRegisteredAdmin
}